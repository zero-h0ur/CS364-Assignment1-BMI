<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <!--
        หน้าจอแนวนอนจัดแบบสองคอลัมน์
        ฝั่งซ้ายเป็นแบบฟอร์ม และฝั่งขวาเป็นผลลัพธ์ BMI
    -->
    <LinearLayout
        android:id="@+id/screen_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:orientation="horizontal"
        android:padding="@dimen/screen_padding">

        <!--
            layout_width เป็น 0dp เพื่อให้ layout_weight
            แบ่งพื้นที่ครึ่งซ้ายและครึ่งขวาเท่ากัน
        -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <include
                layout="@layout/view_bmi_form"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

        </LinearLayout>

        <Space
            android:layout_width="@dimen/section_gap"
            android:layout_height="match_parent" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <include
                layout="@layout/view_bmi_result"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

        </LinearLayout>

    </LinearLayout>

</ScrollView>